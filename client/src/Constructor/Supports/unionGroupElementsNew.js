//Конфиг
import { config, PI, PI_90, SIZE_STANDART } from '../Config.js';

import { getMatrixByPoints, getElemInsideByCell } from '../helpers/constructorHelper.js';

export const unionGroupElementsNew = ( elems, states ) => {

	return {};

}